# Local UI smoke: load Brief against public Kite hosts (FE is MIT; kite.json data is CC BY-NC).
# Copy to .env (gitignored) before `npm run kite`.
VITE_BASE_PATH=https://kite.kagi.com
VITE_STATIC_PATH=https://kite.kagi.com/static
