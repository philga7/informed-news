name: CI

on:
  pull_request:
    branches:
      - main

jobs:
  validate:
    name: MVP build & typecheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install root dependencies
        run: npm ci

      - name: Install MVP server
        run: npm ci --prefix mvp/server

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Kite provenance tests
        run: npm run test:kite

      - name: Install & build archived React feed (Vercel artifact until hosting cutover)
        run: |
          npm ci --prefix _legacy/mvp-web
          npm run build

      # Optional heavier path: install kite + Playwright when apps/kite changes.
      # Full e2e remains local via `npm run test:e2e:kite`.