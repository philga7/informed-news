{
  "buildCommand": "npm run build",
  "outputDirectory": "mvp/web/dist",
  "installCommand": "npm ci && npm ci --prefix mvp/web",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
