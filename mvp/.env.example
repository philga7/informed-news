PORT=3001
# Prefer bcrypt hash in production; plaintext is fine for local MVP.
# Generate: node -e "require('bcryptjs').hash('your-password',10).then(console.log)"
MVP_PASSWORD=change-me
# MVP_PASSWORD_HASH=
SESSION_SECRET=change-me-to-a-long-random-string
OLLAMA_API_KEY=
OLLAMA_MODEL=glm-5.3-flash
CFP_FEED_URL=https://citizenfreepress.com/feed/
FETCH_LIMIT=25
# Optional: cap how many items to process per batch run (defaults to 10 each).
CLASSIFY_BATCH_LIMIT=10
ENRICH_BATCH_LIMIT=10
# Comma-separated X handles for xcancel ingest (optional). Empty = CFP-only, no errors.
# Example: XCANCEL_PROFILES=sentdefender
XCANCEL_PROFILES=
# Optional alternate/additional handles: mvp/data/x-profiles.json (gitignored).
# Shape: { "handles": ["sentdefender"] } — see mvp/data/x-profiles.example.json
XCANCEL_PER_PROFILE_LIMIT=10
XCANCEL_FETCH_DELAY_MS=1500
